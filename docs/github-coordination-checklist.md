# GitHub Coordination Checklist

Status: working checklist  
Checked against the public site and public GitHub surfaces on 2026-04-21.

## Goal

Keep the homepage and the public GitHub profile telling the same story:

- fluorescence analysis researcher
- open-source builder
- note-driven thinker

## Current Snapshot

Public snapshot confirmed during intake:

- GitHub user login: `YSCJRH`
- profile name: `HJR`
- profile bio: currently empty
- profile blog/homepage field: currently empty

Repository description alignment from public API:

- `ai-visibility-auditor`
  - closely aligned with homepage wording
- `codex-via-phone`
  - closely aligned with homepage wording
- `skylattice`
  - closely aligned with homepage wording
- `mirror-sim`
  - wording divergence to review: GitHub emphasizes the Liu Cixin / "The Mirror" framing more than the homepage does
- `create-double-skill`
  - wording divergence to review: GitHub description is broader than the tighter homepage framing

Still not verified in this repo:

- pinned repository order on the public profile
- whether the public profile should expose the homepage URL
- whether the sparse profile presentation is intentional long-term or just temporary

## Homepage Side

Current homepage emphasis:

1. `ai-visibility-auditor`
2. `codex-via-phone`
3. `skylattice`
4. `mirror-sim`
5. `create-double-skill`

Current homepage action paths:

- visit GitHub
- start with selected projects
- return for notes

## GitHub Side

Things to verify manually on the public profile:

- pinned repositories reflect the same priority order as the homepage, or at least do not contradict it
- profile bio and profile README do not drift into a different identity
- repository descriptions stay consistent with the homepage summaries
- the most representative repo remains easy to find from the profile landing view

## Recommended Alignment Order

### 1. Representative project
- make sure `ai-visibility-auditor` is easy to discover from the GitHub profile
- if pinned repositories are edited later, keep it in the first row / first screen

### 2. Supporting build projects
- keep `codex-via-phone` and `skylattice` close behind the representative project
- treat `mirror-sim` and `create-double-skill` as important but more exploratory signals

### 3. Wording consistency
- homepage project cards should not promise more than the repository README promises
- repository descriptions should not undersell a project that the homepage treats as representative
- if one side changes, check the other side in the same pass

### 4. Notes credibility
- do not link notes publicly before at least one reviewed note is ready
- once one note is ready, make sure both homepage and GitHub point to the same canonical location

## Manual Checks To Run Later

- open the public GitHub profile page
- compare pinned repos with homepage project order
- compare repo descriptions with homepage project summaries
- check whether a profile README is needed or already sufficient
- check whether the homepage URL on the GitHub profile still points to the live site
- decide whether `mirror-sim` and `create-double-skill` should move closer to their current GitHub descriptions or closer to their current homepage framing

## Things Intentionally Left Unchanged For Now

- no GitHub profile bio changes were made from this repository
- no pinned repository order was changed from this repository
- no profile README was created yet
- no additional social or contact links were introduced
